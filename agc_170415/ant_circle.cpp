#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <unordered_map>
using namespace std;

int main()
{
	int N;
	int L;
	int T;
	cin >> N>>L>>T;
	int xi;
	int wi;
	vector<int> X;
	vector<int> W;
	for (int i = 0; i < N; ++i)
	{
		cin >> xi;
		cin >> wi;
		X.push_back(xi);
		W.push_back(wi);
	}
	


	while (true)
	{

	}

	return 0;
}