#include <iostream>
#include <cmath>
using namespace std;

int main()
{
	int H, W;
	cin >> H >> W;
	int H_decf = floor(H / 3.0);
	


	int W_decf = floor(W / 3.0);

	int H_decc = ceil(H / 3.0);
	int W_1 = floor(W / 2.0);

	int W_decc = ceil(W / 3.0);

	if (H_dec >= W_dec)
	{

	}
	else
	{

	}


}